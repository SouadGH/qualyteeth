import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qualyteeth-web',
  webDir: '../../dist/apps/qualyteeth-web',
  bundledWebRuntime: false,
};

export default config;
