import { SnakeToCameInterceptor } from './snake-to-came.interceptor';

describe('SnakeToCameInterceptor', () => {
  it('should be defined', () => {
    expect(new SnakeToCameInterceptor()).toBeDefined();
  });
});
