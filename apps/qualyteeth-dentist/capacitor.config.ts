import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qualyteeth-dentist',
  webDir: '../../dist/apps/qualyteeth-dentist',
  bundledWebRuntime: false,
};

export default config;
