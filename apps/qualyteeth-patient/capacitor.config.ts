import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'qualyteeth-patient',
  webDir: '../../dist/apps/qualyteeth-patient',
  bundledWebRuntime: false,
};

export default config;
